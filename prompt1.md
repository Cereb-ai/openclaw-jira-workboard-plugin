你好。请按顺序做 3 件事，然后回报：

1. 跑 `pwd` 确认你的当前工作目录
2. 跑 `ls -la /home/leoclaw/dev/projects/jira-openclaw-plugin/` 看你能看到哪些文件
3. 读 /home/leoclaw/dev/projects/jira-openclaw-plugin/TASK.md 整个文件（用 read tool，**不要用 cat**）

**不要**：
- 不要读 /home/leoclaw/.openclaw/openclaw.json（红线，详见 TASK.md 顶部）
- 不要读 /home/leoclaw/.openclaw/installs.json（红线）
- 不要读 /home/leoclaw/.openclaw/workspace/config/mcporter.json（红线，ATST token 在那里）
- 不要跑 systemctl / openclaw plugins install/uninstall / rm -rf
- 不要探索 /home/leoclaw/.config/opencode/skills/atlassian-tools/（TASK.md 已有完整信息，不需要二次探索）

只做 1+2+3，做完回报。